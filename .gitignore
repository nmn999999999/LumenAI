# Xcode
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/
build/
*.ipa
*.dSYM.zip

# XcodeGen 生成物（由 project.yml 随时重建）
LocalAI.xcodeproj/

# macOS
.DS_Store

# 运行时产物（不应入库）
*.gguf

# Android 构建产物（由 gradle / build_native.sh 重建）
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/app/src/main/jniLibs/
*.apk
*.aab

# llama.cpp 交叉编译产物（由 build_llama_xcframework.sh / android/build_native.sh 重建）
build-ios/
llama.xcframework/

# SSH 桥接静态库（libssh2 + mbedTLS 交叉编译，由 ssh/ 脚本重建）
ssh.xcframework/

# sherpa-onnx (Kokoro TTS) 推理框架 —— 从 GitHub release 下载,勿入库
SherpaOnnxC.xcframework/
sherpa-onnx-*.zip

# 其他项目克隆（勿提交）
kelivo_repo/
icon_gen*/
icon-lab/
icon_appicon_render/
ipa_output/
ipa_stage*/
build-repro/
build-release*/
tests/

# 构建缓存 / 本地状态（勿提交）
-empty-abi-descriptor/
.local/
.workbuddy/
ipa_final/
